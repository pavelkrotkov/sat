"""Training state: attempts, sessions, spacing, the weakness model.

Precious and irreplaceable. None of this can be rebuilt from raw sources -
it lives only in data/satprep.db and its backups.

May import from `satprep.corpus` (a drill is made of questions); the reverse
is forbidden.
"""
